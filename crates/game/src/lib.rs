// pub fn highlight_selected_squad(
    // mut q_squad: Query<(&Selectable, &mut Stroke, &OwnedBy), With<PixelSquad>>,
    // q_player: Query<&Player>,
// ) {
    // for (selectable, mut stroke, owned_by) in q_squad.iter_mut() {
        // if selectable.is_selected {
            // stroke.color = Color::rgb(1.0, 1.0, 1.0);
            // continue;
        // }

        // if let Ok(player) = q_player.get(owned_by.player_id) {
            // stroke.color = player.faction_color
        // }
    // }
// }
